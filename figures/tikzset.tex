
\tikzset{
    box/.style={
    draw,
    minimum height=1cm,
    text centered,
    minimum width=1cm,
    }
}

\tikzset{
    dashed box/.style={
    draw,
    dashed,
    minimum height=1cm,
    minimum width=1cm,
    }
}

\tikzset{
    arrow/.style={-{Latex[length=2mm]}
    }
}

\tikzset{dot/.style = {circle, fill, minimum size=2pt, inner sep=0pt, outer sep=0pt}}

\tikzset{ota/.style = {trapezium, draw, thick, trapezium angle=80, minimum width=2cm,minimum height=1.5cm, rotate=-90}}
